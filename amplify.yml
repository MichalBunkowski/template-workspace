version: 1
applications:
  - frontend:
      appRoot: packages/web
      phases:
        preBuild:
          commands:
            - nvm install --lts
            - cd packages/web
            - npx lerna bootstrap
        build:
          commands:
            - yarn build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
